[package]
name = "wasm4-fake-raytracing"
version = "0.1.0"
edition = "2021"

[lib]
name = "cart"
crate-type = ["cdylib"]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true

